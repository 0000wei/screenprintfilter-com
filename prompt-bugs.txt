BUG FIX: /home/wu/screenprintfilter-com/index.html

Fix these 4 bugs. Read the full file first, then make targeted edits.

## Bug 1: Gallery example click doesn't reprocess
When user has already uploaded an image, then clicks a gallery example, the parameters change but the image is NOT reprocessed. The `loadGalleryExample` function sets slider values but doesn't call `applyHalftone()`. Fix: after setting parameters in `loadGalleryExample`, check if an image is already loaded (currentImage !== null), and if so, call `applyHalftone()` immediately.

## Bug 2: Download always uses display size, not original
When "Original" checkbox is checked in output size, the download should use the ORIGINAL image dimensions (originalWidth x originalHeight), not the display canvas dimensions. Currently `downloadImage()` calls `resultCanvas.toDataURL()` which exports at resultCanvas size. Fix:
- In `downloadImage()`: if `useOriginalSizeCheckbox.checked`, create a TEMPORARY large canvas at originalWidth×originalHeight, apply the halftone effect at that full resolution, then export to PNG.
- This means the download quality matches the original image, not the 500×400 display preview.

## Bug 3: Undo/Redo
Add undo/redo functionality:
- Store a history stack of parameter snapshots (max 20 entries)
- On each slider/control change, push current state to history
- Add Undo/Redo buttons in the toolbar (between "Load Image" and "Download PNG")
- Keyboard shortcuts: Ctrl+Z / Cmd+Z for undo, Ctrl+Shift+Z / Cmd+Shift+Z for redo
- Each snapshot stores: { dotSize, spacing, contrast, brightness, angle, shape, fgColor, bgColor, useOriginalColors, useOriginalSize, outputW, outputH }

Implementation:
```js
const MAX_HISTORY = 20;
let undoStack = [];
let redoStack = [];

function saveState() {
  undoStack.push(getCurrentParams());
  if (undoStack.length > MAX_HISTORY) undoStack.shift();
  redoStack = [];
  updateUndoButtons();
}

function undo() {
  if (undoStack.length === 0) return;
  redoStack.push(getCurrentParams());
  const state = undoStack.pop();
  applyState(state);
  updateUndoButtons();
}

function redo() {
  if (redoStack.length === 0) return;
  undoStack.push(getCurrentParams());
  const state = redoStack.pop();
  applyState(state);
  updateUndoButtons();
}

function getCurrentParams() { /* return object of all params */ }
function applyState(state) { /* set all sliders, update displays, applyHalftone */ }
function updateUndoButtons() { /* enable/disable buttons based on stack */ }
```

Add Undo/Redo buttons:
```html
<button class="btn btn-sm" id="undoBtn" disabled title="Undo (Ctrl+Z)">↩</button>
<button class="btn btn-sm" id="redoBtn" disabled title="Redo (Ctrl+Shift+Z)">↪</button>
```

Save state in ALL change handlers (input/change events on sliders, selects, colors, checkboxes).

## Bug 4: Canvas zoom (magnify) on hover
Add a magnifier/loupe feature for the result canvas:
- When user holds Shift and hovers over the result canvas, show a magnified circle of the area around the cursor
- The magnifier shows a 3x zoomed view of the pixels under the cursor
- It follows the cursor as it moves
- It disappears when Shift is released or mouse leaves canvas
- The magnifier is a circular div (100px diameter) positioned absolutely inside the canvas wrapper, with CSS `clip-path: circle(50px at center)` and its background set to a zoomed canvas section
- Use CSS `image-rendering: pixelated` for crisp zoom

Implementation: on mousemove over resultCanvas, if shiftKey is held:
1. Calculate the pixel coordinates under cursor
2. Draw a zoomed (3x) section of the canvas to a small offscreen canvas
3. Convert to dataURL and set as background of the magnifier div

Add CSS for magnifier:
```css
.magnifier {
  position: absolute;
  width: 100px;
  height: 100px;
  border-radius: 50%;
  border: 2px solid var(--accent);
  box-shadow: 0 0 20px rgba(0,0,0,0.5);
  pointer-events: none;
  z-index: 20;
  display: none;
  background-repeat: no-repeat;
  image-rendering: pixelated;
}
```

CRITICAL: Do NOT modify GA, SEO meta, AI agent files, or the core halftone algorithm structure. Make targeted edits only.
