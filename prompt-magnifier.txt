BUG FIX: /home/wu/screenprintfilter-com/index.html

## Change: Magnifier activates on hover, not Shift

Current behavior: User must hold Shift and hover over result canvas to see the magnifier.
Desired behavior: Magnifier shows automatically when the mouse hovers over the result canvas (no Shift key required).

Changes needed:

1. Remove the Shift key check from the mousemove handler on resultCanvas. The magnifier should appear on ANY hover over the result canvas.
2. Show magnifier on mouseenter, hide on mouseleave.
3. The magnifier should follow the cursor, zooming in on the pixels underneath.

Find the existing magnifier code and modify it so that:
- `mouseenter` on resultCanvas → show magnifier
- `mousemove` on resultCanvas → update magnifier position and zoomed content
- `mouseleave` on resultCanvas → hide magnifier
- No Shift key required

CRITICAL: Do NOT modify GA, SEO meta, AI agent files, or core halftone algorithm.
