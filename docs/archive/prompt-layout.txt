You are working on /home/wu/screenprintfilter-com/index.html. Read the full file first, then make these major layout changes.

## Layout Change: Single Canvas + Right Sidebar

### New layout structure:
```
┌─────────────────────────────────────────────────────────┐
│  Header (logo + nav)                                     │
├─────────────────────────────────────────────────────────┤
│  Toolbar (Load Image, Download, Reset, Undo, Redo,      │
│           Dot, Adjust, Color, Size controls)             │
├──────────────────────────────┬──────────────────────────┤
│                              │  Example Gallery         │
│   Result Canvas              │  (8 preset cards)        │
│   (center, large)            │                          │
│                              │  Click preset → applies  │
│   Zoom: [50%] [75%] [100%]  │  parameters + reprocess   │
│   [150%] [200%] [Fit]       │                          │
│                              │                          │
│   Drag to pan when zoomed    │                          │
│                              │                          │
└──────────────────────────────┴──────────────────────────┘
│  FAQ                          │
│  Footer                       │
└───────────────────────────────┘
```

### Step-by-step changes:

#### 1. Remove the left/right compare layout
The current layout has `compare-left` (Original) and `compare-right` (Halftone Result) side by side with a divider. **Remove all of this.** Replace with a single result canvas.

- Remove `compare-left`, `compareLabel` for Original, `originalCanvas`, `originalPlaceholder`
- Remove `compare-divider` and its handle
- Keep the `resultCanvas` but rename it to `mainCanvas`
- The canvas-wrapper should still have the placeholder (for "Drop image here")

#### 2. Add zoom controls BELOW the canvas
Add a toolbar row below the canvas with zoom buttons:
```html
<div class="zoom-controls">
  <button class="zoom-btn" data-zoom="0.25">25%</button>
  <button class="zoom-btn" data-zoom="0.5">50%</button>
  <button class="zoom-btn active" data-zoom="1">100%</button>
  <button class="zoom-btn" data-zoom="1.5">150%</button>
  <button class="zoom-btn" data-zoom="2">200%</button>
  <button class="zoom-btn" data-zoom="fit">Fit</button>
  <span class="zoom-label" id="zoomLabel">100%</span>
</div>
```

CSS for zoom controls: centered, small buttons, accent color for active state.

Add zoom logic:
- When a zoom button is clicked, set a `currentZoom` variable
- Apply the zoom to the result canvas using CSS `transform: scale(zoom)` 
- The canvas wrapper should have `overflow: auto` so scrollbars appear when zoomed in
- "Fit" mode: calculate zoom to fit canvas in wrapper, maintaining aspect ratio

#### 3. Add click-to-drag panning when zoomed in
When zoom > 1:
- User can click and drag to pan the canvas
- Use `transform-origin: 0 0` on the canvas
- Track `panX` and `panY` state
- On mousedown: record start position
- On mousemove (while dragging): update panX/panY and apply as `translate(panXpx, panYpx)` in the transform
- On mouseup: stop dragging
- Cursor should change to `grab` (default) and `grabbing` (while dragging)

```js
let currentZoom = 1;
let isPanning = false;
let panStartX = 0, panStartY = 0;
let panX = 0, panY = 0;
```

#### 4. Add Example Gallery to right sidebar
Move the example gallery from below the canvas to a right sidebar column.

Change HTML structure:
```html
<main class="main-content">
  <div class="container">
    <div class="content-layout">
      <div class="canvas-column">
        <!-- Canvas area with zoom controls -->
      </div>
      <aside class="sidebar">
        <h3>Example Gallery</h3>
        <div class="gallery-grid" id="galleryGrid">
          <!-- 8 gallery items -->
        </div>
      </aside>
    </div>
  </div>
</main>
```

CSS for sidebar:
- `.content-layout`: display flex, gap 1.5rem
- `.canvas-column`: flex 1
- `.sidebar`: width 260px, flex-shrink 0
- `.sidebar .gallery-grid`: single column
- `.sidebar .gallery-item`: smaller thumbnails (full width, ~80px height)

Keep the gallery generation JS code exactly the same — it creates elements and appends to `galleryGrid`. The CSS change will handle the layout.

#### 5. Keep the original image reference for processing
The `loadImage` function should still store `originalImageData` from an offscreen canvas, but NOT display it in the UI. Just hide the original canvas and keep processing logic intact.

#### 6. Remove unnecessary elements
- Remove `originalCanvas` HTML element
- Remove `compareContainer`, `compareLeft`, `compareDivider`, `compareRight` HTML elements
- Remove the magnifier (no longer needed with zoom controls)
- Keep `canvasPlaceholder` for the initial state

## Important
- Do NOT change: GA, SEO meta, AI agent files, core halftone algorithm, toolbar controls, FAQ, footer
- Do NOT rewrite the entire file — make targeted edits
- After changes, the file must still be valid HTML
